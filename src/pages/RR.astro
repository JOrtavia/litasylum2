
import { getCollection } from "astro:content";
import RRpage from "../../layouts/RRpage.astro";

export const prerender = true;
const { entry } = Astro.props;
const { Content } = await entry.render();
---

<RRpage frontmatter={entry.data}>
    <Content />
</RRpage>
